import React from 'react';
import { Layout } from 'antd';

const { Header } = Layout;

const AppHeader = () => {
    return <Header style={{ background: '#2E8B57', color: '#fff', textAlign: 'center', fontSize: '18px' }}>Aether</Header>;
};

export default AppHeader;
